#pragma once
#include <string>
#include <algorithm> 
#include <functional> 
#include <cctype>
#include <locale>

void ltrim(std::string &s);
void rtrim(std::string &s);
void trim(std::string &s);

void caps(std::string &s);